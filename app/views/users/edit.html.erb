<% provide(:title, "Edit user") %> 
<!-- rails calls provide function and associate :title with "Edit User" -->
<title>Jasper's App <%= yield(:title) %></title>
<!-- executes and inserts the result of yield, title, into the template -->
<h1>Update your profile</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@user) do |f| %>
    <!-- |xyz| means that xyz is a block variable; it's up to the method to know what to do with the block. The form_for is identical to that with the new user, the difference is that Rails knows whether the user is in the system already. When consctructing the form_for(@user), Rails uses POST if @user.new_record? (from Active Record, it's a boolean function obv) is true and PUT is false-->
      <%= render 'shared/error_messages' %>

      <%= f.label :name %>
      <%= f.text_field :name %>

      <%= f.label :email %>
      <%= f.text_field :email %>

      <%= f.label :password %>
      <%= f.password_field :password %>

      <%= f.label :password_confirmation, "Confirm Password" %>
      <%= f.password_field :password_confirmation %>

      <%= f.submit "Save changes", class: "btn btn-large btn-primary" %>
    <% end %>

    <%= gravatar_for @user %>
    <a href="http://gravatar.com/emails">change</a>
  </div>
</div>
